import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Курсы",
  description: "Описание страницы курсы",
};

export default function Courses() {
  return <div>Courses</div>;
}
